[project]
name = "cody"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.2.0",
    "termcolor>=3.3.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
